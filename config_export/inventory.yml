---
all:
  vars:
    controller_admin_user: "admin"
    controller_admin_password: 'Put your vaulted admin password here'
    validate_certs: true
    config_dump_path: "."
  children:
    automationcontroller:
      hosts:
        aap_controller.local.domain:
...
